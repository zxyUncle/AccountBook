<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--自定义万能布局Dialog 背景透明-->
    <style name="MyDilog" parent="Theme.AppCompat.Dialog">
        <!--设置window的窗口背景为透明-->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!--Theme.Design.BottomSheetDialog-->
    <style name="bottomSheetDialog" parent="Theme.Design.BottomSheetDialog">
        <item name="android:windowAnimationStyle">@style/MyDialogTranslate</item>
        <item name="background">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>

        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        //透明度
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.1</item>
    </style>
    <style name="MyDialogTranslate">
        <item name="android:windowEnterAnimation">@anim/translate_top</item>
        <item name="android:windowExitAnimation">@anim/translate_bottom</item>
    </style>

</resources>